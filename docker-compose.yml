version: "3.9"

services:
  mongo:
    image: mongo:7
    container_name: campus_events_mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: campus_events

volumes:
  mongo_data:
